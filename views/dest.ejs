<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Planner</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Akaya+Telivigala&family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700;1,900&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: rgb(32, 225, 180);
        }

        /* 
        h1 {
            color: #333;
            text-align: center;
        }

        form {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: blanchedalmond;
            border-radius: 20px;
        }

        label {
            display: block;
            margin-top: 10px;
        }

        select {
            width: 30%;
            padding: 10px;
            margin-top: 5px;
            box-sizing: border-box;
            margin-bottom: 20px;
        }

        button {
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;

        } */
        img {
            display: inline-block;
            width: 4rem;
        }

        li {
            display: inline;
        }

        .bg-pri {
            background-color: #FF0000;
        }

        .text-col {
            color: #FFD700;
        }

        .btn-center {
            display: block;
            margin: auto;
        }

        .posterImage {
            display: inline-block;
            max-width: 5rem;
            ;
        }

        .navbar-brand {
            font-family: "Merriweather", serif;
            font-weight: 400;
            font-style: normal;
            color: rgb(253, 177, 0);
        }
    </style>
</head>

<body>
    <h1>Travel Planner</h1>

    <div class="container">
        <% for(let dest of destination){ %>
            <hr>
            <div class="posterImage"><img src=<%=dest.photo %> class="img-thumbnail" alt="Poster"></div>

            <li>
                <%= dest.title %> (<%= dest.city %>)
            </li>
            <a href="/api/booking/tourview?title=<%= dest.title %>"><button class="btn btn-info">Details</button></a>

            <% } %>
                <hr>
    </div>


    <script>

    </script>
</body>

</html>